# Neon PostgreSQL connection string
DATABASE_URL=postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require

# Google OAuth credentials (https://console.cloud.google.com/apis/credentials)
NUXT_OAUTH_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
NUXT_OAUTH_GOOGLE_CLIENT_SECRET=your-google-client-secret

# Optional: explicit OAuth redirect URI (auto-detected if not set)
# GOOGLE_REDIRECT_URI=https://logify.whoisarjen.com/api/auth/google/callback

# Vercel Cron secret (set in Vercel dashboard, used to protect /api/cron/cleanup)
CRON_SECRET=your-random-cron-secret
